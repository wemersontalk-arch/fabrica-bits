# Variáveis de Ambiente para Deploy no Vercel / Produção

# Segredo de Assinatura de JWT (Troque por um texto longo e secreto no Vercel)
JWT_SECRET="fabrica_bits_super_secret_jwt_key_2026_master"

# Chave de API da OpenAI (Reside 100% no servidor Vercel, nunca exposta no cliente)
OPENAI_API_KEY="sk-proj-sua-chave-openai-aqui"

# URL do Banco de Dados PostgreSQL (Ex: Supabase, Neon ou PostgreSQL VPS)
DATABASE_URL="postgres://usuario:senha@ep-demo.neon.tech/fabrica_bits?sslmode=require"
